# ECC Templates

`ECC_TEMPLATE.xlsx` is the fixed-layout template for ECC report generation.

It should remain accessible to the MCP server under:

```text
templates/ECC_TEMPLATE.xlsx
```

To regenerate the template from source:

```bash
python3 scripts/build_ecc_template.py
```
